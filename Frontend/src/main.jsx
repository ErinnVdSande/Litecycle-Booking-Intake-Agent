import React from "react";
import ReactDOM from "react-dom/client";
import BookingConsole from "../BookingConsole.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <BookingConsole />
  </React.StrictMode>
);